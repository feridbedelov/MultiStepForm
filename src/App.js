import React from "react";
import "./App.css"

import Form from "./components/Form";

const App = (props) => {
  return (
    <div className = "center-card">
      <Form />
    </div>
  );
};

export default App;
